# Final mode covers

The eight final mode covers live in `public/assets/map-banners/`. Original versions, generated PNG intermediates, conversion previews and the obsolete export script were removed at the user's request. The retained WebP files are the final masters.

The covers use chunky arcade pixels and retain each mode's recognizable scene and objective. Existing catalog URLs remain unchanged.
